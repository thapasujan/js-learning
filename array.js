// arrays

const myArr  = [1,2,4,5]

const myArr2 = new Array(1,2,3,4)

// Array methods

myArr.push(6) //adds the value to the end of the array
myArr.pop() //removes the last value from the array

// .join(): converts the array into a string

// slice, splice
// slice: cuts the array and returns the new array
// splice: cuts the array and returns the removed elements manipulates the original array

// arr.slice(1,3), arr.aplice(1,3)

// arr.concat(arr2)
// arr = [...arr, ...arr2] works same as concat but can be used for more arrays

// for array contains array and array then use .flat(infinity)

// for the values individuals like a= 1, b=2, c=3
// to create array we can use array.of(a, b, c): it returns new array
